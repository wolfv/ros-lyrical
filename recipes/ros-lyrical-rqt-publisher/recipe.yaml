# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-rqt-publisher
  version: 1.10.3
source:
  git: https://github.com/ros2-gbp/rqt_publisher-release.git
  tag: release/lyrical/rqt_publisher/1.10.3-1
  target_directory: ros-lyrical-rqt-publisher/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_python.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_python.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://wiki.ros.org/rqt_publisher
  repository: https://github.com/ros-visualization/rqt_publisher
  license: BSD-3-Clause
  summary: rqt_publisher provides a GUI plugin for publishing arbitrary messages with fixed or computed field values.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - pytest
    - python
    - ros-lyrical-ament-copyright
    - ros-lyrical-ament-flake8
    - ros-lyrical-ament-pep257
    - ros-lyrical-ament-xmllint
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
    - setuptools
  run:
    - numpy
    - python
    - ros-lyrical-ament-index-python
    - ros-lyrical-python-qt-binding
    - ros-lyrical-qt-gui-py-common
    - ros-lyrical-rclpy
    - ros-lyrical-ros-workspace
    - ros-lyrical-rosidl-runtime-py
    - ros-lyrical-rqt-gui
    - ros-lyrical-rqt-gui-py
    - ros-lyrical-rqt-py-common
    - ros2-distro-mutex 0.16.* lyrical_*
