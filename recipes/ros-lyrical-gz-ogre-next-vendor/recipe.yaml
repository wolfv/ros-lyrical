# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-gz-ogre-next-vendor
  version: 0.2.1
source:
  git: https://github.com/ros2-gbp/gz_ogre_next_vendor-release.git
  tag: release/lyrical/gz_ogre_next_vendor/0.2.1-1
  target_directory: ros-lyrical-gz-ogre-next-vendor/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ogre3d.org/
  license: Apache-2.0 OR MIT
  summary: Vendor package for Ogre-next v2.3.3
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - freeimage
    - freetype
    - glslang
    - libboost-devel
    - libvulkan-headers
    - libvulkan-loader
    - numpy
    - ogre-next
    - pip
    - poco
    - python
    - rapidjson
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-cmake-vendor-package
    - ros-lyrical-ament-cmake-xmllint
    - ros-lyrical-ament-lint-auto
    - ros-lyrical-gz-cmake-vendor
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
    - sdl2
    - tbb
    - tbb-devel
    - tinyxml2
    - xorg-libx11
    - xorg-xorgproto
    - zziplib
  run:
    - freeimage
    - freetype
    - glslang
    - libboost-devel
    - libvulkan-headers
    - libvulkan-loader
    - ogre-next
    - poco
    - python
    - rapidjson
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
    - sdl2
    - tbb
    - tbb-devel
    - tinyxml2
    - xorg-libx11
    - xorg-xorgproto
    - zziplib
