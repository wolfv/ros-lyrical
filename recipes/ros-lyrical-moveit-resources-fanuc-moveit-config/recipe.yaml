# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-moveit-resources-fanuc-moveit-config
  version: 3.1.1
source:
  git: https://github.com/ros2-gbp/moveit_resources-release.git
  tag: release/lyrical/moveit_resources_fanuc_moveit_config/3.1.1-3
  target_directory: ros-lyrical-moveit-resources-fanuc-moveit-config/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://moveit.ros.org
  repository: https://github.com/ros-planning/moveit_resources
  license: BSD-3-Clause
  summary: '<p>MoveIt Resources for testing: Fanuc M-10iA.</p><p>A project-internal configuration for testing in MoveIt.</p>'
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-lyrical-ament-cmake
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - python
    - ros-lyrical-controller-manager
    - ros-lyrical-joint-state-publisher
    - ros-lyrical-moveit-resources-fanuc-description
    - ros-lyrical-robot-state-publisher
    - ros-lyrical-ros-workspace
    - ros-lyrical-ros2cli-common-extensions
    - ros-lyrical-tf2-ros
    - ros-lyrical-xacro
    - ros2-distro-mutex 0.16.* lyrical_*
