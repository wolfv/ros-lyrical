# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-rqt-plot
  version: 1.7.5
source:
  git: https://github.com/ros2-gbp/rqt_plot-release.git
  tag: release/lyrical/rqt_plot/1.7.5-3
  target_directory: ros-lyrical-rqt-plot/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_python.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_python.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://wiki.ros.org/rqt_plot
  repository: https://github.com/ros-visualization/rqt_plot
  license: BSD-3-Clause
  summary: rqt_plot provides a GUI plugin visualizing numeric values in a 2D plot using different plotting backends.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - libgl-devel
    - libopengl-devel
    - numpy
    - pip
    - pytest
    - python
    - qt6-main
    - ros-lyrical-ament-copyright
    - ros-lyrical-ament-flake8
    - ros-lyrical-ament-pep257
    - ros-lyrical-ament-xmllint
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-test-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
    - setuptools
  run:
    - matplotlib-base
    - numpy
    - python
    - ros-lyrical-ament-index-python
    - ros-lyrical-python-qt-binding
    - ros-lyrical-qt-gui-py-common
    - ros-lyrical-rclpy
    - ros-lyrical-ros-workspace
    - ros-lyrical-rosidl-parser
    - ros-lyrical-rosidl-runtime-py
    - ros-lyrical-rqt-gui
    - ros-lyrical-rqt-gui-py
    - ros-lyrical-rqt-py-common
    - ros-lyrical-std-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
