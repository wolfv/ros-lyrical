# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-laser-filters
  version: 2.3.2
source:
  git: https://github.com/ros2-gbp/laser_filters-release.git
  tag: release/lyrical/laser_filters/2.3.2-3
  target_directory: ros-lyrical-laser-filters/src/work
  patches:
    - patch/ros-lyrical-laser-filters.win.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/laser_filters
  license: BSD-3-Clause
  summary: Assorted filters designed to operate on 2D planar laser scanners, which use the sensor_msgs/LaserScan type.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-lyrical-ament-cmake-auto
    - ros-lyrical-ament-cmake-gtest
    - ros-lyrical-angles
    - ros-lyrical-diagnostic-msgs
    - ros-lyrical-diagnostic-updater
    - ros-lyrical-filters
    - ros-lyrical-laser-geometry
    - ros-lyrical-launch-testing-ament-cmake
    - ros-lyrical-message-filters
    - ros-lyrical-pluginlib
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-components
    - ros-lyrical-rclcpp-lifecycle
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-sensor-msgs
    - ros-lyrical-tf2
    - ros-lyrical-tf2-geometry-msgs
    - ros-lyrical-tf2-kdl
    - ros-lyrical-tf2-ros
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - python
    - ros-lyrical-angles
    - ros-lyrical-diagnostic-msgs
    - ros-lyrical-diagnostic-updater
    - ros-lyrical-filters
    - ros-lyrical-laser-geometry
    - ros-lyrical-message-filters
    - ros-lyrical-pluginlib
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-components
    - ros-lyrical-rclcpp-lifecycle
    - ros-lyrical-ros-workspace
    - ros-lyrical-sensor-msgs
    - ros-lyrical-tf2
    - ros-lyrical-tf2-geometry-msgs
    - ros-lyrical-tf2-kdl
    - ros-lyrical-tf2-ros
    - ros2-distro-mutex 0.16.* lyrical_*
