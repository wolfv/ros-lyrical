# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-ros2-fmt-logger
  version: 1.1.0
source:
  git: https://github.com/ros2-gbp/ros2_fmt_logger-release.git
  tag: release/lyrical/ros2_fmt_logger/1.1.0-3
  target_directory: ros-lyrical-ros2-fmt-logger/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/nobleo/ros2_fmt_logger
  repository: https://github.com/nobleo/ros2_fmt_logger
  license: Apache-2.0
  summary: A modern, ROS 2 logging library that provides fmt-style formatting as a replacement for RCLCPP logging macros
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - fmt
    - numpy
    - pip
    - python
    - ros-lyrical-ament-cmake-auto
    - ros-lyrical-ament-cmake-gtest
    - ros-lyrical-backward-ros
    - ros-lyrical-rclcpp
    - ros-lyrical-rcutils
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - fmt
    - python
    - ros-lyrical-backward-ros
    - ros-lyrical-rclcpp
    - ros-lyrical-rcutils
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
