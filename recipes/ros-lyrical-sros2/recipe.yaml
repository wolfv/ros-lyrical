# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-sros2
  version: 0.16.6
source:
  git: https://github.com/ros2-gbp/sros2-release.git
  tag: release/lyrical/sros2/0.16.6-1
  target_directory: ros-lyrical-sros2/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_python.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_python.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: Command line tools for managing SROS2 keys
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - pytest
    - pytest-timeout
    - python
    - ros-lyrical-ament-copyright
    - ros-lyrical-ament-flake8
    - ros-lyrical-ament-mypy
    - ros-lyrical-ament-pep257
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-testing
    - ros-lyrical-ros-workspace
    - ros-lyrical-test-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
    - setuptools
  run:
    - argcomplete
    - cryptography
    - lxml
    - python
    - ros-lyrical-ament-index-python
    - ros-lyrical-rclpy
    - ros-lyrical-ros-workspace
    - ros-lyrical-ros2cli
    - ros2-distro-mutex 0.16.* lyrical_*
