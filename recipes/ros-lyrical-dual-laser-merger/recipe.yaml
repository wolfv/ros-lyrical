# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-dual-laser-merger
  version: 0.0.1
source:
  git: https://github.com/ros2-gbp/dual_laser_merger-release.git
  tag: release/lyrical/dual_laser_merger/0.0.1-3
  target_directory: ros-lyrical-dual-laser-merger/src/work
  patches:
    - patch/ros-lyrical-dual-laser-merger.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: merge dual lidar's scans.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen-abi-devel
    - libboost-devel
    - numpy
    - pcl
    - pip
    - python
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-cmake-auto
    - ros-lyrical-ament-copyright
    - ros-lyrical-ament-cpplint
    - ros-lyrical-ament-flake8
    - ros-lyrical-ament-lint-auto
    - ros-lyrical-ament-lint-common
    - ros-lyrical-ament-pep257
    - ros-lyrical-ament-xmllint
    - ros-lyrical-geometry-msgs
    - ros-lyrical-laser-geometry
    - ros-lyrical-message-filters
    - ros-lyrical-pcl-conversions
    - ros-lyrical-pcl-ros
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-components
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-tf2
    - ros-lyrical-tf2-ros
    - ros-lyrical-tf2-sensor-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
    - vtk-base
  run:
    - eigen-abi-devel
    - libboost-devel
    - pcl
    - python
    - ros-lyrical-geometry-msgs
    - ros-lyrical-laser-geometry
    - ros-lyrical-message-filters
    - ros-lyrical-pcl-conversions
    - ros-lyrical-pcl-ros
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-components
    - ros-lyrical-ros-workspace
    - ros-lyrical-tf2
    - ros-lyrical-tf2-ros
    - ros-lyrical-tf2-sensor-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
    - vtk-base
