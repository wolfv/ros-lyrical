# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-rviz-ogre-vendor
  version: 15.2.3
source:
  git: https://github.com/ros2-gbp/rviz-release.git
  tag: release/lyrical/rviz_ogre_vendor/15.2.3-1
  target_directory: ros-lyrical-rviz-ogre-vendor/src/work
  patches:
    - patch/ros-lyrical-rviz-ogre-vendor.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ogre3d.org/
  license: Apache-2.0 OR MIT
  summary: Wrapper around ogre3d, it provides a fixed CMake module and an ExternalProject build of ogre.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - vcstool
  host:
    - ${{ 'libglu' if linux }}
    - assimp
    - if: build_platform == target_platform
      then:
        - pkg-config
    - freeimage
    - freetype
    - glew
    - numpy
    - pip
    - pugixml
    - python
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-cmake-vendor-package
    - ros-lyrical-ament-cmake-xmllint
    - ros-lyrical-ament-lint-auto
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
    - xorg-libx11
    - xorg-xorgproto
    - zlib
    - zziplib
  run:
    - assimp
    - freetype
    - glew
    - python
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
    - xorg-libx11
    - xorg-xorgproto
