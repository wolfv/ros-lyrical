# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-plotjuggler-ros
  version: 2.3.1
source:
  git: https://github.com/ros2-gbp/plotjuggler-ros-plugins-release.git
  tag: release/lyrical/plotjuggler_ros/2.3.1-3
  target_directory: ros-lyrical-plotjuggler-ros/src/work
  patches:
    - patch/ros-lyrical-plotjuggler-ros.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/facontidavide/PlotJuggler
  license: AGPL-3.0-only
  summary: PlotJuggler plugin for ROS
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: build_platform != target_platform
      then:
        - qt-main
  host:
    - ${{ 'libgl-devel' if linux }}
    - ${{ 'libopengl-devel' if linux }}
    - binutils
    - if: build_platform == target_platform
      then:
        - pkg-config
    - fmt
    - libboost-devel
    - libboost-python-devel
    - libgl-devel
    - libopengl-devel
    - numpy
    - pip
    - python
    - qt-main
    - ros-lyrical-ament-cmake
    - ros-lyrical-plotjuggler
    - ros-lyrical-plotjuggler-msgs
    - ros-lyrical-rclcpp
    - ros-lyrical-rcpputils
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-rosbag2-transport
    - ros-lyrical-tf2-msgs
    - ros-lyrical-tf2-ros
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - binutils
    - fmt
    - libboost-devel
    - libboost-python-devel
    - libgl-devel
    - libopengl-devel
    - python
    - qt-main
    - ros-lyrical-plotjuggler
    - ros-lyrical-plotjuggler-msgs
    - ros-lyrical-rclcpp
    - ros-lyrical-rcpputils
    - ros-lyrical-ros-workspace
    - ros-lyrical-rosbag2-transport
    - ros-lyrical-tf2-msgs
    - ros-lyrical-tf2-ros
    - ros2-distro-mutex 0.16.* lyrical_*
