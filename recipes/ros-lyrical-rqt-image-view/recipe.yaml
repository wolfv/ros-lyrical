# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-rqt-image-view
  version: 2.0.5
source:
  git: https://github.com/ros2-gbp/rqt_image_view-release.git
  tag: release/lyrical/rqt_image_view/2.0.5-3
  target_directory: ros-lyrical-rqt-image-view/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://wiki.ros.org/rqt_image_view
  repository: https://github.com/ros-visualization/rqt_image_view
  license: BSD-3-Clause
  summary: rqt_image_view provides a GUI plugin for displaying images using image_transport.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - ${{ 'libgl-devel' if linux }}
    - ${{ 'libopengl-devel' if linux }}
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - qt6-main
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-lint-auto
    - ros-lyrical-ament-lint-common
    - ros-lyrical-cv-bridge
    - ros-lyrical-geometry-msgs
    - ros-lyrical-image-transport
    - ros-lyrical-qt-gui-cpp
    - ros-lyrical-rclcpp
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-rqt-gui
    - ros-lyrical-rqt-gui-cpp
    - ros-lyrical-sensor-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - python
    - qt6-main
    - ros-lyrical-cv-bridge
    - ros-lyrical-geometry-msgs
    - ros-lyrical-image-transport
    - ros-lyrical-qt-gui-cpp
    - ros-lyrical-rclcpp
    - ros-lyrical-ros-workspace
    - ros-lyrical-rqt-gui
    - ros-lyrical-rqt-gui-cpp
    - ros-lyrical-sensor-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
