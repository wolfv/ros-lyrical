# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-control-toolbox
  version: 6.3.0
source:
  git: https://github.com/ros2-gbp/control_toolbox-release.git
  tag: release/lyrical/control_toolbox/6.3.0-3
  target_directory: ros-lyrical-control-toolbox/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://control.ros.org
  repository: https://github.com/ros-controls/control_toolbox/
  license: Apache-2.0
  summary: The control toolbox contains modules that are useful across all controllers.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - cpp-expected
    - eigen
    - fmt
    - numpy
    - pip
    - python
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-cmake-gmock
    - ros-lyrical-backward-ros
    - ros-lyrical-control-msgs
    - ros-lyrical-filters
    - ros-lyrical-generate-parameter-library
    - ros-lyrical-geometry-msgs
    - ros-lyrical-pluginlib
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-lifecycle
    - ros-lyrical-rcutils
    - ros-lyrical-realtime-tools
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-ros2-control-cmake
    - ros-lyrical-rsl
    - ros-lyrical-tf2
    - ros-lyrical-tf2-geometry-msgs
    - ros-lyrical-tf2-ros
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - cpp-expected
    - eigen
    - python
    - ros-lyrical-backward-ros
    - ros-lyrical-control-msgs
    - ros-lyrical-filters
    - ros-lyrical-generate-parameter-library
    - ros-lyrical-geometry-msgs
    - ros-lyrical-pluginlib
    - ros-lyrical-rclcpp
    - ros-lyrical-rcutils
    - ros-lyrical-realtime-tools
    - ros-lyrical-ros-workspace
    - ros-lyrical-rsl
    - ros-lyrical-tf2
    - ros-lyrical-tf2-geometry-msgs
    - ros-lyrical-tf2-ros
    - ros2-distro-mutex 0.16.* lyrical_*
tests:
  - script:
      - cmake-package-check control_toolbox --targets control_toolbox::control_toolbox control_toolbox::rate_limiter control_toolbox::low_pass_filter
    requirements:
      run:
        - cmake-package-check
        - ${{ compiler('c') }}
        - ${{ compiler('cxx') }}
