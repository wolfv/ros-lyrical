# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-lanelet2-traffic-rules
  version: 1.2.1
source:
  git: https://github.com/ros2-gbp/lanelet2-release.git
  tag: release/lyrical/lanelet2_traffic_rules/1.2.1-8
  target_directory: ros-lyrical-lanelet2-traffic-rules/src/work
  patches:
    - patch/ros-lyrical-lanelet2-traffic-rules.win.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  repository: https://github.com/fzi-forschungszentrum-informatik/lanelet2.git
  license: BSD-3-Clause
  summary: Package for interpreting traffic rules in a lanelet map
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - gtest
    - numpy
    - pip
    - python
    - ros-lyrical-ament-cmake-core
    - ros-lyrical-lanelet2-core
    - ros-lyrical-mrt-cmake-modules
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - python
    - ros-lyrical-lanelet2-core
    - ros-lyrical-mrt-cmake-modules
    - ros-lyrical-ros-workspace
    - ros2-distro-mutex 0.16.* lyrical_*
