# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-geometric-shapes
  version: 2.3.3
source:
  git: https://github.com/ros2-gbp/geometric_shapes-release.git
  tag: release/lyrical/geometric_shapes/2.3.3-3
  target_directory: ros-lyrical-geometric-shapes/src/work
  patches:
    - patch/ros-lyrical-geometric-shapes.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/geometric_shapes
  license: BSD-3-Clause
  summary: This package contains generic definitions of geometric shapes and bodies.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: target_platform == 'emscripten-wasm32'
      then:
        - ros-lyrical-rosidl-default-generators
  host:
    - assimp
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - fcl
    - libboost-devel
    - numpy
    - octomap
    - pip
    - pkg-config
    - python
    - qhull
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-cmake-copyright
    - ros-lyrical-ament-cmake-gtest
    - ros-lyrical-ament-lint-auto
    - ros-lyrical-ament-lint-cmake
    - ros-lyrical-console-bridge-vendor
    - ros-lyrical-eigen-stl-containers
    - ros-lyrical-eigen3-cmake-module
    - ros-lyrical-geometry-msgs
    - ros-lyrical-random-numbers
    - ros-lyrical-rclcpp
    - ros-lyrical-resource-retriever
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-rosidl-default-generators
    - ros-lyrical-shape-msgs
    - ros-lyrical-visualization-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
  run:
    - assimp
    - eigen
    - libboost
    - libboost-devel
    - python
    - qhull
    - ros-lyrical-console-bridge-vendor
    - ros-lyrical-eigen-stl-containers
    - ros-lyrical-eigen3-cmake-module
    - ros-lyrical-geometry-msgs
    - ros-lyrical-random-numbers
    - ros-lyrical-rclcpp
    - ros-lyrical-resource-retriever
    - ros-lyrical-ros-workspace
    - ros-lyrical-rosidl-default-runtime
    - ros-lyrical-shape-msgs
    - ros-lyrical-visualization-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
