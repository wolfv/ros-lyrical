# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-lyrical-velodyne-pointcloud
  version: 2.5.1
source:
  git: https://github.com/ros2-gbp/velodyne-release.git
  tag: release/lyrical/velodyne_pointcloud/2.5.1-3
  target_directory: ros-lyrical-velodyne-pointcloud/src/work
  patches:
    - patch/ros-lyrical-velodyne-pointcloud.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 20
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  repository: https://github.com/ros-drivers/velodyne
  license: BSD-3-Clause
  summary: Point cloud conversions for Velodyne 3D LIDARs.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - eigen-abi-devel
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - numpy
    - pcl
    - pip
    - python
    - ros-lyrical-ament-cmake
    - ros-lyrical-ament-cmake-gtest
    - ros-lyrical-ament-index-cpp
    - ros-lyrical-ament-lint-auto
    - ros-lyrical-ament-lint-common
    - ros-lyrical-angles
    - ros-lyrical-diagnostic-updater
    - ros-lyrical-geometry-msgs
    - ros-lyrical-message-filters
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-components
    - ros-lyrical-ros-environment
    - ros-lyrical-ros-workspace
    - ros-lyrical-sensor-msgs
    - ros-lyrical-tf2
    - ros-lyrical-tf2-ros
    - ros-lyrical-velodyne-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
    - vtk-base
    - yaml-cpp
  run:
    - eigen
    - eigen-abi-devel
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - pcl
    - python
    - ros-lyrical-angles
    - ros-lyrical-diagnostic-updater
    - ros-lyrical-geometry-msgs
    - ros-lyrical-message-filters
    - ros-lyrical-rclcpp
    - ros-lyrical-rclcpp-components
    - ros-lyrical-ros-workspace
    - ros-lyrical-sensor-msgs
    - ros-lyrical-tf2
    - ros-lyrical-tf2-ros
    - ros-lyrical-velodyne-msgs
    - ros2-distro-mutex 0.16.* lyrical_*
    - vtk-base
    - yaml-cpp
